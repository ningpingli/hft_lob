# Shared dataset-level baseline experiment configuration.
loader:
  batch_size: 128
  num_workers: 0
  pin_memory: false
  persistent_workers: false
  prefetch_factor: null

baselines:
  names: [ridge]
  ridge_alpha: 1.0

evaluation:
  prediction_bins: 10

folds:
  start_fold: 1
  num_folds: null

seed: 42
